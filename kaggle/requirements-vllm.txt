# Inference-only environment (often separate from training due to torch pins).
# Install per vLLM docs for your CUDA version: https://docs.vllm.ai/en/latest/getting_started/installation.html
vllm>=0.6.0
pandas
numpy
tqdm
